{% extends 'ApiAdvertBundle:Default:index.html.twig' %}

{% block container %}
    <h1>Ville</h1>
    <hr>
    <table class="table table-hover table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
        </tbody>
    </table>
    <hr>
    <div class="pull-right">
        {{ form_start(delete_form) }}
        <a class="btn btn-default" href="{{ path('city_index') }}" role="button">Retour à la liste</a>
        <a class="btn btn-info" href="{{ path('city_edit', { 'id': city.id }) }}" role="button">Editer</a>
        {{ form_start(delete_form) }}
        {{ form_widget(delete_form.Supprimer, {'attr': {'class': 'btn btn-danger'}} ) }}
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
