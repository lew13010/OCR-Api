{% extends 'ApiAdvertBundle:Default:index.html.twig' %}

{% block container %}
    <h1>Categories</h1>
    <hr>
    <table class="table table-hover table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>
    <hr>
    <div class="pull-right">
        {{ form_start(delete_form) }}
            <a class="btn btn-default" href="{{ path('category_index') }}" role="button">Retour à la liste</a>
            <a class="btn btn-info" href="{{ path('category_edit', { 'id': category.id }) }}" role="button">Editer</a>
            {{ form_widget(delete_form.Supprimer, {'attr': {'class': 'btn btn-danger'}} ) }}
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
